<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 
          http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>rar</id>
    <formats>
        <format>rar</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <!-- 1. Include the SHADED JAR (not the original) -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>${project.artifactId}-${project.version}-shaded.jar</include>
            </includes>
            <excludes>
                <exclude>${project.artifactId}-${project.version}.jar</exclude>
            </excludes>
        </fileSet>
        
        <!-- 2. Include META-INF with ra.xml -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources/META-INF</directory>
            <outputDirectory>/META-INF</outputDirectory>
            <includes>
                <include>ra.xml</include>
                <include>MANIFEST.MF</include>
            </includes>
        </fileSet>
        
        <!-- 3. Optional: Create lib directory with BouncyCastle -->
        <fileSet>
            <directory>${project.build.directory}/dependency</directory>
            <outputDirectory>/lib</outputDirectory>
            <includes>
                <include>**/bouncycastle*.jar</include>
            </includes>
        </fileSet>
    </fileSets>
    
    <!-- 4. Optional: Dependency sets -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
            <scope>runtime</scope>
            <excludes>
                <exclude>${project.groupId}:${project.artifactId}</exclude>
                <exclude>org.glassfish.jersey:*</exclude>
                <exclude>javax.*:*</exclude>
                <exclude>org.bouncycastle:*</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>
</assembly>