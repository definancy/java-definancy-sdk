/*
 * Definancy API
 * REST API for managing asset, network and contract configurations, and vault-based financial operations. This API provides a comprehensive set of endpoints for health monitoring, network management, asset  configuration, contract management, vault lifecycle management, and QR-Code generation. It is designed to facilitate seamless integration with financial systems, ensuring secure and efficient payment  processing while maintaining strict compliance standards.
 *
 * The version of the OpenAPI document: 0.0.1.wip.20250925
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.definancy.model;

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.util.Objects;
import java.util.StringJoiner;

/**
 * Government-issued identification document details for individual identity verification including
 * document number, type, and issuing authority.
 */
@JsonPropertyOrder({
    PersonNaturalId.JSON_PROPERTY_NUMBER,
    PersonNaturalId.JSON_PROPERTY_TYPE,
    PersonNaturalId.JSON_PROPERTY_ISSUER
})
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.20.0")
public class PersonNaturalId {
    public static final String JSON_PROPERTY_NUMBER = "number";
    @javax.annotation.Nonnull private String number;

    public static final String JSON_PROPERTY_TYPE = "type";
    @javax.annotation.Nonnull private PersonNaturalIdType type;

    public static final String JSON_PROPERTY_ISSUER = "issuer";
    @javax.annotation.Nonnull private String issuer;

    public PersonNaturalId() {}

    /** Constructor with all args parameters */
    public PersonNaturalId(
            @JsonProperty(value = JSON_PROPERTY_NUMBER, required = true) String number,
            @JsonProperty(value = JSON_PROPERTY_TYPE, required = true) PersonNaturalIdType type,
            @JsonProperty(value = JSON_PROPERTY_ISSUER, required = true) String issuer) {
        this.number = number;
        this.type = type;
        this.issuer = issuer;
    }

    public PersonNaturalId number(@javax.annotation.Nonnull String number) {

        this.number = number;
        return this;
    }

    /**
     * Identification document number.
     *
     * @return number
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_NUMBER, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public String getNumber() {
        return number;
    }

    @JsonProperty(value = JSON_PROPERTY_NUMBER, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setNumber(@javax.annotation.Nonnull String number) {
        this.number = number;
    }

    public PersonNaturalId type(@javax.annotation.Nonnull PersonNaturalIdType type) {

        this.type = type;
        return this;
    }

    /**
     * Get type
     *
     * @return type
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_TYPE, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public PersonNaturalIdType getType() {
        return type;
    }

    @JsonProperty(value = JSON_PROPERTY_TYPE, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setType(@javax.annotation.Nonnull PersonNaturalIdType type) {
        this.type = type;
    }

    public PersonNaturalId issuer(@javax.annotation.Nonnull String issuer) {

        this.issuer = issuer;
        return this;
    }

    /**
     * Identification issuing authority.
     *
     * @return issuer
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_ISSUER, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public String getIssuer() {
        return issuer;
    }

    @JsonProperty(value = JSON_PROPERTY_ISSUER, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setIssuer(@javax.annotation.Nonnull String issuer) {
        this.issuer = issuer;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PersonNaturalId personNaturalId = (PersonNaturalId) o;
        return Objects.equals(this.number, personNaturalId.number)
                && Objects.equals(this.type, personNaturalId.type)
                && Objects.equals(this.issuer, personNaturalId.issuer);
    }

    @Override
    public int hashCode() {
        return Objects.hash(number, type, issuer);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class PersonNaturalId {\n");
        sb.append("    number: ").append(toIndentedString(number)).append("\n");
        sb.append("    type: ").append(toIndentedString(type)).append("\n");
        sb.append("    issuer: ").append(toIndentedString(issuer)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    /**
     * Convert the instance into URL query string.
     *
     * @return URL query string
     */
    public String toUrlQueryString() {
        return toUrlQueryString(null);
    }

    /**
     * Convert the instance into URL query string.
     *
     * @param prefix prefix of the query string
     * @return URL query string
     */
    public String toUrlQueryString(String prefix) {
        String suffix = "";
        String containerSuffix = "";
        String containerPrefix = "";
        if (prefix == null) {
            // style=form, explode=true, e.g. /pet?name=cat&type=manx
            prefix = "";
        } else {
            // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
            prefix = prefix + "[";
            suffix = "]";
            containerSuffix = "]";
            containerPrefix = "[";
        }

        StringJoiner joiner = new StringJoiner("&");

        // add `number` to the URL query string
        if (getNumber() != null) {
            try {
                joiner.add(
                        String.format(
                                java.util.Locale.ROOT,
                                "%snumber%s=%s",
                                prefix,
                                suffix,
                                URLEncoder.encode(String.valueOf(getNumber()), "UTF-8")
                                        .replaceAll("\\+", "%20")));
            } catch (UnsupportedEncodingException e) {
                // Should never happen, UTF-8 is always supported
                throw new RuntimeException(e);
            }
        }

        // add `type` to the URL query string
        if (getType() != null) {
            try {
                joiner.add(
                        String.format(
                                java.util.Locale.ROOT,
                                "%stype%s=%s",
                                prefix,
                                suffix,
                                URLEncoder.encode(String.valueOf(getType()), "UTF-8")
                                        .replaceAll("\\+", "%20")));
            } catch (UnsupportedEncodingException e) {
                // Should never happen, UTF-8 is always supported
                throw new RuntimeException(e);
            }
        }

        // add `issuer` to the URL query string
        if (getIssuer() != null) {
            try {
                joiner.add(
                        String.format(
                                java.util.Locale.ROOT,
                                "%sissuer%s=%s",
                                prefix,
                                suffix,
                                URLEncoder.encode(String.valueOf(getIssuer()), "UTF-8")
                                        .replaceAll("\\+", "%20")));
            } catch (UnsupportedEncodingException e) {
                // Should never happen, UTF-8 is always supported
                throw new RuntimeException(e);
            }
        }

        return joiner.toString();
    }

    public static class Builder {

        private PersonNaturalId instance;

        public Builder() {
            this(new PersonNaturalId());
        }

        protected Builder(PersonNaturalId instance) {
            this.instance = instance;
        }

        public PersonNaturalId.Builder number(String number) {
            this.instance.number = number;
            return this;
        }

        public PersonNaturalId.Builder type(PersonNaturalIdType type) {
            this.instance.type = type;
            return this;
        }

        public PersonNaturalId.Builder issuer(String issuer) {
            this.instance.issuer = issuer;
            return this;
        }

        /**
         * returns a built PersonNaturalId instance.
         *
         * <p>The builder is not reusable.
         */
        public PersonNaturalId build() {
            try {
                return this.instance;
            } finally {
                // ensure that this.instance is not reused
                this.instance = null;
            }
        }

        @Override
        public String toString() {
            return getClass() + "=(" + instance + ")";
        }
    }

    /** Create a builder with no initialized field. */
    public static PersonNaturalId.Builder builder() {
        return new PersonNaturalId.Builder();
    }

    /** Create a builder with a shallow copy of this instance. */
    public PersonNaturalId.Builder toBuilder() {
        return new PersonNaturalId.Builder()
                .number(getNumber())
                .type(getType())
                .issuer(getIssuer());
    }
}
