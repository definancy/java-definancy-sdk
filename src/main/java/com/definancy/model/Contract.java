/*
 * Definancy API
 * REST API for managing asset, network and contract configurations, and vault-based financial operations. This API provides a comprehensive set of endpoints for health monitoring, network management, asset  configuration, contract management, vault lifecycle management, and QR-Code generation. It is designed to facilitate seamless integration with financial systems, ensuring secure and efficient payment  processing while maintaining strict compliance standards.
 *
 * The version of the OpenAPI document: 0.0.1.wip.20250925
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.definancy.model;

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import java.util.Objects;
import java.util.StringJoiner;

/**
 * Complete contract definition combining identification, metadata, and configuration. Represents a
 * deployed contract that can process payments for a specific asset on a specific blockchain
 * network.
 */
@JsonPropertyOrder({
    Contract.JSON_PROPERTY_ID,
    Contract.JSON_PROPERTY_INFO,
    Contract.JSON_PROPERTY_CONFIG
})
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.20.0")
public class Contract {
    public static final String JSON_PROPERTY_ID = "id";
    @javax.annotation.Nonnull private ContractId id;

    public static final String JSON_PROPERTY_INFO = "info";
    @javax.annotation.Nonnull private ContractInfo info;

    public static final String JSON_PROPERTY_CONFIG = "config";
    @javax.annotation.Nonnull private ContractConfig config;

    public Contract() {}

    /** Constructor with all args parameters */
    public Contract(
            @JsonProperty(value = JSON_PROPERTY_ID, required = true) ContractId id,
            @JsonProperty(value = JSON_PROPERTY_INFO, required = true) ContractInfo info,
            @JsonProperty(value = JSON_PROPERTY_CONFIG, required = true) ContractConfig config) {
        this.id = id;
        this.info = info;
        this.config = config;
    }

    public Contract id(@javax.annotation.Nonnull ContractId id) {

        this.id = id;
        return this;
    }

    /**
     * Get id
     *
     * @return id
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_ID, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public ContractId getId() {
        return id;
    }

    @JsonProperty(value = JSON_PROPERTY_ID, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setId(@javax.annotation.Nonnull ContractId id) {
        this.id = id;
    }

    public Contract info(@javax.annotation.Nonnull ContractInfo info) {

        this.info = info;
        return this;
    }

    /**
     * Get info
     *
     * @return info
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_INFO, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public ContractInfo getInfo() {
        return info;
    }

    @JsonProperty(value = JSON_PROPERTY_INFO, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setInfo(@javax.annotation.Nonnull ContractInfo info) {
        this.info = info;
    }

    public Contract config(@javax.annotation.Nonnull ContractConfig config) {

        this.config = config;
        return this;
    }

    /**
     * Get config
     *
     * @return config
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_CONFIG, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public ContractConfig getConfig() {
        return config;
    }

    @JsonProperty(value = JSON_PROPERTY_CONFIG, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setConfig(@javax.annotation.Nonnull ContractConfig config) {
        this.config = config;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        Contract contract = (Contract) o;
        return Objects.equals(this.id, contract.id)
                && Objects.equals(this.info, contract.info)
                && Objects.equals(this.config, contract.config);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id, info, config);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class Contract {\n");
        sb.append("    id: ").append(toIndentedString(id)).append("\n");
        sb.append("    info: ").append(toIndentedString(info)).append("\n");
        sb.append("    config: ").append(toIndentedString(config)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    /**
     * Convert the instance into URL query string.
     *
     * @return URL query string
     */
    public String toUrlQueryString() {
        return toUrlQueryString(null);
    }

    /**
     * Convert the instance into URL query string.
     *
     * @param prefix prefix of the query string
     * @return URL query string
     */
    public String toUrlQueryString(String prefix) {
        String suffix = "";
        String containerSuffix = "";
        String containerPrefix = "";
        if (prefix == null) {
            // style=form, explode=true, e.g. /pet?name=cat&type=manx
            prefix = "";
        } else {
            // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
            prefix = prefix + "[";
            suffix = "]";
            containerSuffix = "]";
            containerPrefix = "[";
        }

        StringJoiner joiner = new StringJoiner("&");

        // add `id` to the URL query string
        if (getId() != null) {
            joiner.add(getId().toUrlQueryString(prefix + "id" + suffix));
        }

        // add `info` to the URL query string
        if (getInfo() != null) {
            joiner.add(getInfo().toUrlQueryString(prefix + "info" + suffix));
        }

        // add `config` to the URL query string
        if (getConfig() != null) {
            joiner.add(getConfig().toUrlQueryString(prefix + "config" + suffix));
        }

        return joiner.toString();
    }

    public static class Builder {

        private Contract instance;

        public Builder() {
            this(new Contract());
        }

        protected Builder(Contract instance) {
            this.instance = instance;
        }

        public Contract.Builder id(ContractId id) {
            this.instance.id = id;
            return this;
        }

        public Contract.Builder info(ContractInfo info) {
            this.instance.info = info;
            return this;
        }

        public Contract.Builder config(ContractConfig config) {
            this.instance.config = config;
            return this;
        }

        /**
         * returns a built Contract instance.
         *
         * <p>The builder is not reusable.
         */
        public Contract build() {
            try {
                return this.instance;
            } finally {
                // ensure that this.instance is not reused
                this.instance = null;
            }
        }

        @Override
        public String toString() {
            return getClass() + "=(" + instance + ")";
        }
    }

    /** Create a builder with no initialized field. */
    public static Contract.Builder builder() {
        return new Contract.Builder();
    }

    /** Create a builder with a shallow copy of this instance. */
    public Contract.Builder toBuilder() {
        return new Contract.Builder().id(getId()).info(getInfo()).config(getConfig());
    }
}
