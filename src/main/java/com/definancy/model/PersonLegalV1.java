/*
 * Definancy API
 * REST API for managing asset, network and contract configurations, and vault-based financial operations. This API provides a comprehensive set of endpoints for health monitoring, network management, asset  configuration, contract management, vault lifecycle management, and QR-Code generation. It is designed to facilitate seamless integration with financial systems, ensuring secure and efficient payment  processing while maintaining strict compliance standards.
 *
 * The version of the OpenAPI document: 0.0.1.wip.20250925
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.definancy.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.util.Objects;
import java.util.StringJoiner;

/** Legal entity (organization) details. */
@JsonPropertyOrder({PersonLegalV1.JSON_PROPERTY_ID, PersonLegalV1.JSON_PROPERTY_INCORPORATION})
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.20.0")
@JsonIgnoreProperties(
        value = "type", // ignore manually set type, it will be automatically generated by Jackson
        // during serialization
        allowSetters = true // allows the type to be set during deserialization
        )
@JsonTypeInfo(
        use = JsonTypeInfo.Id.NAME,
        include = JsonTypeInfo.As.PROPERTY,
        property = "type",
        visible = true)
public class PersonLegalV1 extends PersonBase {
    public static final String JSON_PROPERTY_ID = "id";
    @javax.annotation.Nonnull private PersonLegalId id;

    public static final String JSON_PROPERTY_INCORPORATION = "incorporation";
    @javax.annotation.Nonnull private PersonLegalIncorporation incorporation;

    public PersonLegalV1() {}

    /** Constructor with all args parameters */
    public PersonLegalV1(
            @JsonProperty(value = JSON_PROPERTY_ID, required = true) PersonLegalId id,
            @JsonProperty(value = JSON_PROPERTY_INCORPORATION, required = true)
                    PersonLegalIncorporation incorporation,
            @JsonProperty(value = JSON_PROPERTY_TYPE, required = true) PersonType type,
            @JsonProperty(value = JSON_PROPERTY_NAME, required = true) String name,
            @JsonProperty(value = JSON_PROPERTY_ADDRESS, required = true) PersonAddress address) {
        super(type, name, address);
        this.id = id;
        this.incorporation = incorporation;
    }

    public PersonLegalV1 id(@javax.annotation.Nonnull PersonLegalId id) {

        this.id = id;
        return this;
    }

    /**
     * Get id
     *
     * @return id
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_ID, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public PersonLegalId getId() {
        return id;
    }

    @JsonProperty(value = JSON_PROPERTY_ID, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setId(@javax.annotation.Nonnull PersonLegalId id) {
        this.id = id;
    }

    public PersonLegalV1 incorporation(
            @javax.annotation.Nonnull PersonLegalIncorporation incorporation) {

        this.incorporation = incorporation;
        return this;
    }

    /**
     * Get incorporation
     *
     * @return incorporation
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_INCORPORATION, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public PersonLegalIncorporation getIncorporation() {
        return incorporation;
    }

    @JsonProperty(value = JSON_PROPERTY_INCORPORATION, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setIncorporation(@javax.annotation.Nonnull PersonLegalIncorporation incorporation) {
        this.incorporation = incorporation;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PersonLegalV1 personLegalV1 = (PersonLegalV1) o;
        return Objects.equals(this.id, personLegalV1.id)
                && Objects.equals(this.incorporation, personLegalV1.incorporation)
                && super.equals(o);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id, incorporation, super.hashCode());
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class PersonLegalV1 {\n");
        sb.append("    ").append(toIndentedString(super.toString())).append("\n");
        sb.append("    id: ").append(toIndentedString(id)).append("\n");
        sb.append("    incorporation: ").append(toIndentedString(incorporation)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    /**
     * Convert the instance into URL query string.
     *
     * @return URL query string
     */
    public String toUrlQueryString() {
        return toUrlQueryString(null);
    }

    /**
     * Convert the instance into URL query string.
     *
     * @param prefix prefix of the query string
     * @return URL query string
     */
    public String toUrlQueryString(String prefix) {
        String suffix = "";
        String containerSuffix = "";
        String containerPrefix = "";
        if (prefix == null) {
            // style=form, explode=true, e.g. /pet?name=cat&type=manx
            prefix = "";
        } else {
            // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
            prefix = prefix + "[";
            suffix = "]";
            containerSuffix = "]";
            containerPrefix = "[";
        }

        StringJoiner joiner = new StringJoiner("&");

        // add `type` to the URL query string
        if (getType() != null) {
            try {
                joiner.add(
                        String.format(
                                java.util.Locale.ROOT,
                                "%stype%s=%s",
                                prefix,
                                suffix,
                                URLEncoder.encode(String.valueOf(getType()), "UTF-8")
                                        .replaceAll("\\+", "%20")));
            } catch (UnsupportedEncodingException e) {
                // Should never happen, UTF-8 is always supported
                throw new RuntimeException(e);
            }
        }

        // add `name` to the URL query string
        if (getName() != null) {
            try {
                joiner.add(
                        String.format(
                                java.util.Locale.ROOT,
                                "%sname%s=%s",
                                prefix,
                                suffix,
                                URLEncoder.encode(String.valueOf(getName()), "UTF-8")
                                        .replaceAll("\\+", "%20")));
            } catch (UnsupportedEncodingException e) {
                // Should never happen, UTF-8 is always supported
                throw new RuntimeException(e);
            }
        }

        // add `address` to the URL query string
        if (getAddress() != null) {
            joiner.add(getAddress().toUrlQueryString(prefix + "address" + suffix));
        }

        // add `id` to the URL query string
        if (getId() != null) {
            joiner.add(getId().toUrlQueryString(prefix + "id" + suffix));
        }

        // add `incorporation` to the URL query string
        if (getIncorporation() != null) {
            joiner.add(getIncorporation().toUrlQueryString(prefix + "incorporation" + suffix));
        }

        return joiner.toString();
    }

    public static class Builder extends PersonBase.Builder {

        private PersonLegalV1 instance;

        public Builder() {
            this(new PersonLegalV1());
        }

        protected Builder(PersonLegalV1 instance) {
            super(instance);
            this.instance = instance;
        }

        public PersonLegalV1.Builder id(PersonLegalId id) {
            this.instance.id = id;
            return this;
        }

        public PersonLegalV1.Builder incorporation(PersonLegalIncorporation incorporation) {
            this.instance.incorporation = incorporation;
            return this;
        }

        public PersonLegalV1.Builder type(PersonType type) { // inherited: true
            super.type(type);
            return this;
        }

        public PersonLegalV1.Builder name(String name) { // inherited: true
            super.name(name);
            return this;
        }

        public PersonLegalV1.Builder address(PersonAddress address) { // inherited: true
            super.address(address);
            return this;
        }

        /**
         * returns a built PersonLegalV1 instance.
         *
         * <p>The builder is not reusable.
         */
        public PersonLegalV1 build() {
            try {
                return this.instance;
            } finally {
                // ensure that this.instance is not reused
                super.build();
                this.instance = null;
            }
        }

        @Override
        public String toString() {
            return getClass() + "=(" + instance + ")";
        }
    }

    /** Create a builder with no initialized field. */
    public static PersonLegalV1.Builder builder() {
        return new PersonLegalV1.Builder();
    }

    /** Create a builder with a shallow copy of this instance. */
    public PersonLegalV1.Builder toBuilder() {
        return new PersonLegalV1.Builder()
                .type(getType())
                .name(getName())
                .address(getAddress())
                .id(getId())
                .incorporation(getIncorporation());
    }
}
