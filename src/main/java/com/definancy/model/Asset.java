/*
 * Definancy API
 * REST API for managing asset, network and contract configurations, and vault-based financial operations. This API provides a comprehensive set of endpoints for health monitoring, network management, asset  configuration, contract management, vault lifecycle management, and QR-Code generation. It is designed to facilitate seamless integration with financial systems, ensuring secure and efficient payment  processing while maintaining strict compliance standards.
 *
 * The version of the OpenAPI document: 0.0.1.wip.20250925
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.definancy.model;

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.util.Objects;
import java.util.StringJoiner;

/**
 * Complete digital asset definition combining identification, metadata, and configuration.
 * Represents a supported asset that can be used in payment processing across multiple networks.
 */
@JsonPropertyOrder({Asset.JSON_PROPERTY_UNIT, Asset.JSON_PROPERTY_INFO, Asset.JSON_PROPERTY_CONFIG})
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.20.0")
public class Asset {
    public static final String JSON_PROPERTY_UNIT = "unit";
    @javax.annotation.Nonnull private String unit;

    public static final String JSON_PROPERTY_INFO = "info";
    @javax.annotation.Nonnull private AssetInfo info;

    public static final String JSON_PROPERTY_CONFIG = "config";
    @javax.annotation.Nonnull private AssetConfig config;

    public Asset() {}

    /** Constructor with all args parameters */
    public Asset(
            @JsonProperty(value = JSON_PROPERTY_UNIT, required = true) String unit,
            @JsonProperty(value = JSON_PROPERTY_INFO, required = true) AssetInfo info,
            @JsonProperty(value = JSON_PROPERTY_CONFIG, required = true) AssetConfig config) {
        this.unit = unit;
        this.info = info;
        this.config = config;
    }

    public Asset unit(@javax.annotation.Nonnull String unit) {

        this.unit = unit;
        return this;
    }

    /**
     * Ticker symbol for digital asset.
     *
     * @return unit
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_UNIT, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public String getUnit() {
        return unit;
    }

    @JsonProperty(value = JSON_PROPERTY_UNIT, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setUnit(@javax.annotation.Nonnull String unit) {
        this.unit = unit;
    }

    public Asset info(@javax.annotation.Nonnull AssetInfo info) {

        this.info = info;
        return this;
    }

    /**
     * Get info
     *
     * @return info
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_INFO, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public AssetInfo getInfo() {
        return info;
    }

    @JsonProperty(value = JSON_PROPERTY_INFO, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setInfo(@javax.annotation.Nonnull AssetInfo info) {
        this.info = info;
    }

    public Asset config(@javax.annotation.Nonnull AssetConfig config) {

        this.config = config;
        return this;
    }

    /**
     * Get config
     *
     * @return config
     */
    @javax.annotation.Nonnull
    @JsonProperty(value = JSON_PROPERTY_CONFIG, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public AssetConfig getConfig() {
        return config;
    }

    @JsonProperty(value = JSON_PROPERTY_CONFIG, required = true)
    @JsonInclude(value = JsonInclude.Include.ALWAYS)
    public void setConfig(@javax.annotation.Nonnull AssetConfig config) {
        this.config = config;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        Asset asset = (Asset) o;
        return Objects.equals(this.unit, asset.unit)
                && Objects.equals(this.info, asset.info)
                && Objects.equals(this.config, asset.config);
    }

    @Override
    public int hashCode() {
        return Objects.hash(unit, info, config);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class Asset {\n");
        sb.append("    unit: ").append(toIndentedString(unit)).append("\n");
        sb.append("    info: ").append(toIndentedString(info)).append("\n");
        sb.append("    config: ").append(toIndentedString(config)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    /**
     * Convert the instance into URL query string.
     *
     * @return URL query string
     */
    public String toUrlQueryString() {
        return toUrlQueryString(null);
    }

    /**
     * Convert the instance into URL query string.
     *
     * @param prefix prefix of the query string
     * @return URL query string
     */
    public String toUrlQueryString(String prefix) {
        String suffix = "";
        String containerSuffix = "";
        String containerPrefix = "";
        if (prefix == null) {
            // style=form, explode=true, e.g. /pet?name=cat&type=manx
            prefix = "";
        } else {
            // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
            prefix = prefix + "[";
            suffix = "]";
            containerSuffix = "]";
            containerPrefix = "[";
        }

        StringJoiner joiner = new StringJoiner("&");

        // add `unit` to the URL query string
        if (getUnit() != null) {
            try {
                joiner.add(
                        String.format(
                                java.util.Locale.ROOT,
                                "%sunit%s=%s",
                                prefix,
                                suffix,
                                URLEncoder.encode(String.valueOf(getUnit()), "UTF-8")
                                        .replaceAll("\\+", "%20")));
            } catch (UnsupportedEncodingException e) {
                // Should never happen, UTF-8 is always supported
                throw new RuntimeException(e);
            }
        }

        // add `info` to the URL query string
        if (getInfo() != null) {
            joiner.add(getInfo().toUrlQueryString(prefix + "info" + suffix));
        }

        // add `config` to the URL query string
        if (getConfig() != null) {
            joiner.add(getConfig().toUrlQueryString(prefix + "config" + suffix));
        }

        return joiner.toString();
    }

    public static class Builder {

        private Asset instance;

        public Builder() {
            this(new Asset());
        }

        protected Builder(Asset instance) {
            this.instance = instance;
        }

        public Asset.Builder unit(String unit) {
            this.instance.unit = unit;
            return this;
        }

        public Asset.Builder info(AssetInfo info) {
            this.instance.info = info;
            return this;
        }

        public Asset.Builder config(AssetConfig config) {
            this.instance.config = config;
            return this;
        }

        /**
         * returns a built Asset instance.
         *
         * <p>The builder is not reusable.
         */
        public Asset build() {
            try {
                return this.instance;
            } finally {
                // ensure that this.instance is not reused
                this.instance = null;
            }
        }

        @Override
        public String toString() {
            return getClass() + "=(" + instance + ")";
        }
    }

    /** Create a builder with no initialized field. */
    public static Asset.Builder builder() {
        return new Asset.Builder();
    }

    /** Create a builder with a shallow copy of this instance. */
    public Asset.Builder toBuilder() {
        return new Asset.Builder().unit(getUnit()).info(getInfo()).config(getConfig());
    }
}
